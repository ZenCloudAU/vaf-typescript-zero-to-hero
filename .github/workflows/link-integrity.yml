name: Link Integrity

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  check:
    uses: ZenCloudAU/velocity-architecture/.github/workflows/link-integrity.yml@main
    with:
      site_url: 'https://zencloudau.github.io/vaf-typescript-zero-to-hero/'
      repo_name: 'vaf-typescript-zero-to-hero'
