// Lesson 01: Hello TypeScript

const practitionerName: string = "Phil";
const courseName: string = "TypeScript Zero to Hero";

console.log(`Welcome ${practitionerName} to ${courseName}.`);

const message: string =
  "TypeScript helps vibe coders move from guessing to controlled delivery.";

console.log(message);
